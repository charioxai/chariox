# run cli
export ARROBA_OPENCODE_PORT=43111
export ARROBA_OPENCODE_BIN=/Users/miguel/.opencode/bin/opencode
cargo run --manifest-path apps/daemon/Cargo.toml --bin arroba-cli

cargo run --manifest-path apps/daemon/Cargo.toml --bin arroba-cli -- --kernel-url ws://127.0.0.1:43119/kernel

# kill all arroba-related processes
pkill -f 'apps/daemon/target/debug/arroba-daemon'
pkill -f 'apps/daemon/target/debug/arroba-cli'
