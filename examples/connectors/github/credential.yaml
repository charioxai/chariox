id: github-api
description: GitHub REST API token
source:
  type: vault
  key: github-api-token
allowed_hosts:
  - api.github.com
allowed_uses:
  - connector
injection:
  kind: header
  name: Authorization
  value: "Bearer ${secret}"
