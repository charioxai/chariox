id: postgres-local
description: Local Postgres password
source:
  type: vault
  key: postgres-local-password
allowed_hosts:
  - 127.0.0.1:5432
allowed_uses:
  - connector
injection:
  kind: basic
  username: postgres
