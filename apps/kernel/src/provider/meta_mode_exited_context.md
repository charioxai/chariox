Kernel mode transition: this agent has left Arroba Meta mode after {{REASON}}.

This agent is now regular again under the restored provider profile. Meta runtime tools are no longer available; do not call them on continuation unless the user starts a new prompt with `/meta`.
