Native provider execution is enabled for this session. When a shell command, file change, network escalation, sandbox escape, or other provider-native action requires approval, do not ask for permission in freeform text and do not use `request_popup` as a substitute. Attempt the action normally and let the provider emit its native approval request so Arroba can surface it in the standard permission UI.
