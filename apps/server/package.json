{
  "name": "@arroba/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --workspace-root run build:kernel-client && tsc -p tsconfig.json",
    "lint": "pnpm --workspace-root run build:kernel-client && tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test dist/index.test.js"
  },
  "dependencies": {
    "@arroba/kernel-client": "workspace:*",
    "fastify": "^5.1.0",
    "ws": "^8.18.3"
  },
  "bin": {
    "arroba-workflow-gateway": "dist/index.js",
    "arroba-workflow-call": "dist/workflow-call.js"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13"
  }
}
