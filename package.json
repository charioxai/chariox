{
  "name": "arroba",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r --workspace-concurrency=1 run build",
    "start:kernel": "node scripts/start-kernel.mjs",
    "build:kernel-client": "pnpm --filter @arroba/tool-display run build && pnpm --filter @arroba/kernel-client run build",
    "build:cli": "pnpm --filter @arroba/cli run build",
    "lint": "pnpm -r --workspace-concurrency=1 run lint",
    "start:cli": "node scripts/start-cli.mjs",
    "test": "node --test scripts/*.test.mjs && pnpm -r --workspace-concurrency=1 run test",
    "test:daemon": "cargo test --manifest-path apps/kernel/Cargo.toml",
    "perf:drills": "node scripts/performance-drills.mjs",
    "validation:suite": "node apps/cli/scripts/drill-validation-suite.mjs",
    "validation:focused-runtime-gate": "pnpm --filter @arroba/cli run validation:focused-runtime-gate",
    "validation:distributed-runtime-gate": "pnpm --filter @arroba/cli run validation:distributed-runtime-gate",
    "protocol:conformance": "cargo test --manifest-path apps/kernel/Cargo.toml client_protocol_conformance -- --nocapture",
    "smoke:daemon": "cargo run --manifest-path apps/kernel/Cargo.toml --bin arroba-kernel-harness",
    "start:shell": "pnpm --filter @arroba/shell run start"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.10.1"
  }
}
